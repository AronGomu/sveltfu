export class Ressources {
  unc: number; // uncolor cost
  red: number;
  blue: number;
  green: number;

  constructor(
    unc: number,
    red: number,
    blue: number,
    green: number,
  ) {
    this.unc = unc;
    this.red = red;
    this.blue = blue;
    this.green = green;
  }
}
